/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

					Hello! This is readme-file to use project.

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

	This project can been built with maven & jetty only. All you have to do is:
		- Download maven here -> http://maven.apache.org/download.html
		- Configure maven using this -> http://www.apache-maven.ru/install.html
	
	After maven is configurated you can build project using command prompt:
		- go to my-project-web folder
			cd {your download dir}/Google_Tours_Web/my-project/my-project-web
		- build project
			mvn clean install
		- run project 
			mvn jetty:run
		- open localhost:8080/demo/main in your browser
		
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

	File named "dump.sql" is for Data Base Tables restoring.
	If you use either PostgreSQL or MySQL, do the following steps:
		- open command prompt
		- go to "bin" directory of PostgreSQL folder
		- restore Data Base
			psql [dbname] < dump_psql.sql
			or
			mysql [dbname] < dump_mysql.sql
	Due to this command you build 4 tables: users, routes, userroutes and routevalues. 
		- "users" and "userroutes" are empty
		- "routes" and "routevalues" keep information about existing routes
		
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

	Sequence_diagram folder contains picture and sourse-file of prject object's interactions arranged in time sequence

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

	Install SDK:
		mvn install:install-file -DgroupId=com.yota.top.sdk -DartifactId=top-sdk 
		-Dversion=0.1-SNAPSHOT -Dfile=C:/Users/DSavin/Desktop/top-sdk-0.1-SNAPSHOT.jar 
		-Dpackaging=jar -DgeneratePom=true
		
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////		

	SOME DIF
	
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////	